#!/bin/sh

# This file should be copied outside of this repository, typically to
# ~/.nj-tabsetsrc, and then customized.  The file can then be imported via
# the `source` command prior to running the API daemon.

export NETJUMP_IO__TABSETS__ADMIN_PASS="Some Password"
